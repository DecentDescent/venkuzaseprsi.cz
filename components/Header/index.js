import Head from "../Head";
import styles from "./Header.scss";
import classnames from "classnames";

const Header = () => (
  <header classNames={styles["header"]}>venku zase prší</header>
);

export default Header;
